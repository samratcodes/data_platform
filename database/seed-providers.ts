import type { NodeData } from "../types/provider";

// Demonstration providers imported by `npm run db:seed`. The live catalogue is
// populated only by approved registered companies, so this list is empty.
export const nodes: NodeData[] = [];
