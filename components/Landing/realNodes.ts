import type { CityData, NodeData } from "./types";

// The provider catalogue is populated only by approved registered companies.
export const nodes: NodeData[] = [];
export const countries: string[] = [];
export const cities: CityData[] = [];
